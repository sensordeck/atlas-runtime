# Portal Render Contract
# PR57 Customer Trust Surface
# Atlas DSIL SDK
# Status: DRAFT FOR RATIFICATION

---

## 1. Purpose

portal_render_payload.json defines the:

presentation-layer rendering contract
for Atlas Customer Trust Surface artifacts.

The Portal Render Contract exists to ensure:

- deterministic organizational rendering
- limitation-preserved visibility
- replay-backed governance rendering
- cross-role trust consistency
- comparison-boundary preservation

The Portal Render Contract is NOT:

- a governance engine
- a deployment authorization engine
- a certification system
- an AI scoring engine
- a hidden ranking system

Portal rendering is:

presentation infrastructure only.

中文说明：

Portal Render Contract：

只负责展示层。

不是：

决策系统。

---

## 2. Rendering Principles

All portal rendering MUST remain:

- replay-backed
- lineage-preserved
- limitation-aware
- deterministic
- ambiguity-preserving
- governance-oriented
- boundary-explicit

Portal rendering MUST preserve:

- UNKNOWN
- FAIL
- limitation references
- admissibility boundaries
- replay boundaries
- unresolved ambiguity
- candidate findings

Portal rendering MUST NOT generate fake certainty.

---

## 3. Required Rendering Inputs

portal_render_payload.json MAY render:

- replay-visible findings
- governance windows
- runtime comparison findings
- limitation disclosures
- admissibility summaries
- role relevance surfaces
- deterministic replay references
- organizational governance summaries

Portal rendering MUST preserve source lineage.

All rendered findings MUST remain traceable.

---

## 4. Required Rendering Sections

Portal rendering SHOULD support:

- executive summary
- engineering findings
- replay admissibility summary
- governance comparison visibility
- limitation disclosure
- role relevance matrix
- runtime governance questions
- replay lineage references

Portal rendering MAY support:

- OEM evaluation rendering
- supplier comparison rendering
- governance timeline rendering
- runtime window rendering

Portal rendering MUST NOT reinterpret evidence semantics.

---

## 5. Limitation Rendering Rules

Portal rendering MUST visibly preserve:

- FAIL states
- UNKNOWN states
- sparse metadata
- limitation references
- admissibility limitations
- unresolved ambiguity
- candidate findings
- metadata gaps

PR57 MUST preserve limitations.

中文说明：

Portal 不允许：

隐藏 limitation。

---

## 6. Replay Rendering Boundary

Portal rendering MAY display:

- replay hashes
- deterministic regeneration references
- replay visibility
- admissibility references
- runtime lineage
- governance lineage

Replay admissibility
≠
causal proof.

中文说明：

Replay admissibility：

表示“可重复重放一致性”。

不是：

最终因果证明。

Portal MUST NOT render replay as:

- final proof
- root-cause certainty
- legal evidence authority

---

## 7. Governance Rendering Boundary

Portal rendering MAY describe:

- replay-visible governance deviations
- governance mismatches
- limitation deltas
- admissibility deltas
- runtime disturbance visibility
- recovery behavior visibility
- baseline differences

Portal rendering MUST NOT declare:

- supplier fault
- OEM fault
- certification invalidation
- legal liability
- guaranteed root-cause

Comparison visibility
≠
adjudication.

中文说明：

Portal 提供：

“治理对比可见性”。

不是：

责任裁决系统。

---

## 8. Organizational Rendering Boundary

Portal rendering MAY organize content by:

- Engineering
- QA
- CTO
- Deployment
- Supplier
- Certification
- Support

Role rendering exists for:

organizational navigation only.

Role rendering MUST NOT generate:

- responsibility assignment
- blame assignment
- deployment authorization
- legal interpretation

---

## 9. Runtime-Only Rendering Boundary

Portal rendering MUST preserve that:

runtime-only Atlas operation is:

- non-invasive
- observational
- replay-backed
- externally separable
- governance-oriented

Runtime-only mode does NOT require:

- firmware replacement
- sensor replacement
- pipeline migration
- production rewrite
- Fusion dependency

Runtime-only Atlas operation is observational.

中文说明：

Runtime-only 模式：

属于观察层，
不侵入 OEM 原系统。

---

## 10. Fusion Reference Rendering Boundary

Fusion Reference Domain is optional.

Fusion acts as:

trust acceleration infrastructure.

Portal rendering MUST NOT imply:

- mandatory Fusion adoption
- required hardware migration
- required deployment dependency

Fusion MAY be rendered as:

- optional authority boundary enhancement
- optional comparison reference domain
- optional deterministic capture surface

中文说明：

Fusion：

是增强信任边界的基础设施，
不是强制依赖。

---

## 11. Forbidden Rendering Semantics

Portal rendering MUST NOT generate:

- legal conclusions
- supplier blame
- OEM blame
- deployment recommendations
- certification pass/fail
- production readiness declarations
- authority promotion
- AI trust scores
- hidden ranking systems
- confidence scores
- guaranteed root-cause declarations

Portal rendering MUST remain:

boundary-explicit and limitation-preserved.

---

## 12. Hidden Logic Restriction

portal_render_payload.json MUST NOT contain:

- hidden scoring logic
- AI ranking logic
- deployment scoring
- certification scoring
- trust scoring
- production readiness scoring
- hidden interpretation engines

All rendered semantics MUST remain:

traceable to source governance artifacts.

中文说明：

Portal payload：

禁止隐藏：

- AI ranking
- trust score
- hidden scoring system

---

## 13. Deterministic Rendering Requirement

Portal rendering MUST remain:

- deterministic
- regeneration-stable
- lineage-traceable
- semantically stable

Rendering outputs MUST NOT vary semantically
given identical source lineage.

Portal rendering MUST NOT mutate:

- replay semantics
- governance semantics
- limitation boundaries
- admissibility boundaries

---

## 14. Relationship to Earlier PRs

PR53:

runtime governance extraction

PR54:

governance semantics

PR55:

OEM lean-forward governance narrative

PR56:

cross-domain governance comparison

PR57:

organizational trust surface rendering

Portal rendering is:

presentation-layer governance infrastructure.

---

## 15. Final Boundary

Portal rendering provides:

replay-backed organizational trust visibility.

Portal rendering does NOT provide:

- final truth authority
- legal adjudication
- deployment authorization
- certification authority
- production authorization

The purpose of the Portal Render Contract is:

to ensure deterministic and limitation-preserved organizational rendering of Atlas governance artifacts.

中文说明：

Portal 的最终目标：

是帮助组织：

理解 replay-backed governance evidence。

不是：

替组织做最终决策。

---